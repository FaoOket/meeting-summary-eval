transformers>=4.44
accelerate>=0.33
librosa>=0.10
torch>=2.0
# Nécessaires uniquement pour la démo interactive (app.py) :
faster-whisper>=1.0.3
streamlit>=1.38.0
sentencepiece
# Aucune dépendance supplémentaire pour l'évaluation elle-même (WER, couverture,
# agrégats sont implémentés en Python pur dans evaluate.py).
